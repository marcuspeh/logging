# Default API base URL baked into the Vite build.
#
# Override per-developer in .env.local (gitignored), per-shell with
# `export VITE_API_URL=...`, or per-build with a docker compose arg
# (see docker-compose.yml).
VITE_API_URL=http://genmachine:4665
