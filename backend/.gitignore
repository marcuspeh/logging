# Parquet files live on an absolute host path (see docker-compose.yml).
# Nothing to ignore here — that path is outside the repo.

# Go build artefacts
/logging-backend
*.test
*.out

# IDE / OS
.idea/
.vscode/
.DS_Store